// Responsabilidad: Modelo de una capa de parallax del fondo (recursos y propiedades visuales).
// Parte del Model; no renderiza ni actualiza posiciones aquí.

class ParallaxLayer {
  constructor() {
    // Constructor básico sin campos definidos.
  }
}
