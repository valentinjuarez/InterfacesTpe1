// Responsabilidad: Contener parámetros de configuración (velocidades, dimensiones, dificultad, etc.).
// Parte del Model; no contiene lógica, solo valores de configuración.

class GameConfig {
  constructor() {
    // Constructor básico sin asignaciones de configuración aún.
  }
}
