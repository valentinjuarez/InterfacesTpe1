// Responsabilidad: Gestionar la entrada (teclado, mouse, touch) y traducirla a acciones del juego.
// Capa Controller; no registra listeners ni mapea acciones aún.

class InputController {
  constructor() {
    // Constructor básico sin configuración de entradas.
  }
}
